# StackWeave Backend

This folder contains the backend scaffold for the StackWeave project.

## Quick start

1. Open a terminal in `stackweave-backend`
2. Run `npm install`
3. Run `npm run dev`

## Available routes

- `GET /api/health` - health check
- `POST /api/echo` - echo request body
